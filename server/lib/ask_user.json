{   
    "type": "function",
    "function": {
        "name": "ask_user",
        "description": "Interupt the workflow of actions to ask a user a question. This should only be used when you are stuck on a page, or if you absolutely need clarification on the task.",
        "parameters": {
        "type": "object",
        "properties": {
            "prompt": {
            "type": "string",
            "description": "the question to ask the user"
            }
        },
        "required": [
            "prompt"
        ]
        }
    }
}